(() => {
    let flash: {
        name: string;
        age?: number;
        powers: string[];
        getName?: () => string;
    } = {
        name: "Barry Allen",
        age: 24,
        powers: ["Puede correr muy rapido", "Viaja por el tiempo"],
    };
    let superman: {
        name: string;
        age?: number;
        powers: string[];
        getName?: () => string;
    } = {
        name: "Clark Kent",
        age: 24,
        powers: ["Puede volar", "Súper fuerza"],
    };

    })();
